import { AbbvTitlePipe } from './abbv-title.pipe';

describe('AbbvTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new AbbvTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
